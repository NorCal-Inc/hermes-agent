# Central Provider Model Aliases
# All Hermes routing, config, tests, docs, and scripts MUST reference these aliases.
# Do not hardcode dated model IDs like grok-4.20-reasoning or xai/grok-4.20-reasoning anywhere.
# Update this file when models change; never scatter IDs.

grok_reasoning_primary: grok-4.20-0309-reasoning
grok_builder: grok-build-0.1
grok_fast: grok-4.3
# Add more as needed, e.g.
# claude_primary: claude-3-7-sonnet-20250219
# etc.

# Usage in config.yaml:
# model:
#   default: grok_reasoning_primary
# provider_routing:
#   reasoning: grok_reasoning_primary
